# Git
.git
.gitignore
.github

# Virtualenv Windows bị commit nhầm vào repo - vô dụng trong container Linux
.venv

# Dependencies: để npm ci / pip install dựng lại trong image
node_modules
__pycache__
*.pyc

# Secrets - tuyệt đối không đưa vào image
.env
.env.*
!.env.example

# Database dump - chứa dữ liệu vận hành thật
.sql
*.sql

# Build outputs
dist
build
coverage
*.log

# Thư mục tạm khi chạy
uploads
tmp
temp

# File Docker không cần nằm trong build context
Dockerfile
Dockerfile.*
docker-compose*.yml
.dockerignore

# Tài liệu
DEPLOY.md
README.md
*.md
.docs

# Apps Script của dự án khác, không thuộc backend này
update_funtions

# Credentials
firebase-notification.json
nofication-xnk-firebase-adminsdk-*

# Bản sao repo lồng nhau (nếu có)
.BE_XNK
BE_XNK

# IDE / OS
.vscode
.idea
.DS_Store
Thumbs.db
